- Refine listeners
- Update poll interface


- More permissions
  - Deleting lists
  - Closing polls
  - Managing triggers
  - PinThat


- Language combine enum and default values?
- Do not delete triggering messages
- Spin up instance react listener for polls
- Messages larger than the character maximum for fields/description (see Storytime)

- Do reactions trigger the correct tally command?

- When pre-processing, just check if the command being run exists. Validate should care about the entire pattern.
  - Ex. "tally search x" fails with a command not found, because x is < 3 chars, so the pattern fails.

- Command stats public?

- Delete generic success messages after a period of time
  - React
  - Simon Says
